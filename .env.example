# Eclari Environment Variables
# Copy this file to .env and fill in your actual values

# Flask Configuration
FLASK_SECRET_KEY=your-very-secure-secret-key-change-this

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-service-key-here
SUPABASE_ANON_KEY=your-supabase-anon-public-key-here

# Instructions:
# 1. Go to https://supabase.com and create a new project
# 2. In your Supabase dashboard, go to Settings > API
# 3. Copy the URL and paste it as SUPABASE_URL
# 4. Copy the "service_role" key and paste it as SUPABASE_KEY (this is for server-side operations)
# 5. Copy the "anon public" key and paste it as SUPABASE_ANON_KEY (this is for frontend)
# 6. Generate a strong secret key for FLASK_SECRET_KEY

# Make sure your Supabase Row Level Security (RLS) is configured properly!
# Users should have a role field in their raw_user_meta_data or user_metadata